
#include "gtest/gtest.h"

#include "chapter5.6.2_towers.hpp"

namespace SCO {
// 30. Assume that the monks in Hanoi can move 1 disk per minute (they are in no
// hurry to finish the job because employment opportunities for people with this
// particular skill are limited in Hanoi). How long will it take them to solve
// the entire 64-disk problem? Express your result in years.
// TEST(TowersTest, Move64Towers) {
//  int moveCount = towers(64, 1, 3);
//  // Each move takes 1 minute
//  long long totalMinutes = moveCount;
//  // Convert minutes to years
//  double totalYears = totalMinutes / (60.0 * 24.0 * 365.0);
//  std::print("Total moves: {}\n", moveCount);
//  std::print("Total years: {:.2e}\n", totalYears);
//}

} // namespace SCO